/**
 * This function does nothing and it is meant to be used
 * in functional programming approach as a safe callback.
 * @param args - (optional)
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function noop(args?: unknown): void {
  // does nothing
}
